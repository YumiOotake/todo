html css

最初表示処理から書く
　Route コントローラ　　テーブル作成　http://localhost:8080にアクセス　モデル
保存メソッド
ルート　コントローラでは受け取り、保存、戻る。戻る時データ持っていかない。indexページではそもそも全部表示にするから

viewメソッド　index返す時全部DBから持ってくる。行追加して表示→DBから全部持ってきて表示はforeachで

編集削除
ボタン押したら一緒にID持ってく
その場で


cssはあとで